{
  "name": "excel-interviewer-frontend-tests",
  "version": "1.0.0",
  "description": "E2E tests for Excel Interviewer frontend",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
